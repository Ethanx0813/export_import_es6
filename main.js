// main.js
import { factorial } from './utils.js';

// Example: Calculate and display the factorial of a given number
const numberToCalculate = 5;
const result = factorial(numberToCalculate);

console.log(`Factorial of ${numberToCalculate} is: ${result}`);
